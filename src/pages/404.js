import { Flex, Heading } from '@chakra-ui/react';

const NotFoundPage = () => (
  <Flex minH="100vh" flexGrow={1} justifyContent="center" alignItems="center">
    <Heading>404 Not Found</Heading>
  </Flex>
);

export default NotFoundPage;
